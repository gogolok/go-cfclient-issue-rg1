This repo illustrates the change in Organization.Suspend type from `*bool` to `bool`.

If you set up the environment variables
CF_API
CF_USERNAME
CF_PASSWORD
you could run the demo code and hopefully see everything is still fine.

```
$ go run main.go
```
